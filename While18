#include "pt4.h"
using namespace std;

void Solve()
{
    Task("While18");
    int n,x,s=0,i=0;
    pt >> n;
    while (n > 0) {
        x = n % 10;
        n = n / 10;
        s = s + x;
        i++;
    }pt << i << s;

}
